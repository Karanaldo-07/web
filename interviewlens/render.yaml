services:
  - type: web
    name: interviewlens-api
    runtime: python
    rootDir: interviewlens/backend
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: BRAVE_SEARCH_API_KEY
        sync: false
